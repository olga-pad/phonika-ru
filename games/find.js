'use strict';
document.addEventListener('DOMContentLoaded',()=>{
 const G=window.PhonikaGames,{shuffle,createDoubleExposureTasks}=G,childTabs=document.getElementById('childTabs'),gamesTab=document.getElementById('gamesTab'),gamesGrid=document.getElementById('gamesGrid'),findGame=document.getElementById('findGame');
 const findCard=document.getElementById('findGameCard'),soundsMode=document.getElementById('findSoundsMode'),wordsMode=document.getElementById('findWordsMode'),answers=document.getElementById('findAnswers'),findPicture=document.getElementById('findPicture'),allGames=document.getElementById('allGames'),findDinoSteps=document.getElementById('findDinoSteps'),findDino=document.getElementById('findDino'),findPrev=document.getElementById('findPrev'),findNext=document.getElementById('findNext');
 let findCompletion=null,isFindOpen=false,findMode='sounds',findTaskIndex=0,wordFindSession=[],soundFindSession=[],wordFindOriginal=[],soundFindOriginal=[],continueTimer=0;
 const prep=task=>{task.attempted=false;task.completed=false;task.hadWrongAttempt=false;return task;};
 const hideFindCompletion=()=>{if(findCompletion)findCompletion.hidden=true;if(findGame)findGame.hidden=false;};
 const createWordFindSession=()=>{const currentCards=[...new Set((sessionWords||[]).slice(0,wordSessionSize))],allWords=[...new Map(Object.values(curriculum).flat().map(([word,picture])=>[word,{word,picture}])).values()],byWord=new Map(allWords.map(item=>[item.word,item]));wordFindOriginal=createDoubleExposureTasks(currentCards,allWords.map(item=>item.word),(targetWord,distractor)=>({targetWord,picture:byWord.get(targetWord)?.picture||'',distractor,answers:shuffle([targetWord,distractor]),attempted:false,completed:false,hadWrongAttempt:false,rewarded:false,closed:false}));wordFindSession=[...wordFindOriginal];};
 const createSoundFindSession=()=>{const currentSounds=[...new Set((letterSession||[]).slice(0,letterSessionSize))],allSounds=letters.map(([sound])=>sound);soundFindOriginal=createDoubleExposureTasks(currentSounds,allSounds,(targetSound,distractor)=>{const letter=letters.find(([sound])=>sound===targetSound);return{targetSound,picture:letter?.[2]||'',spoken:letter?.[1]||targetSound,distractor,answers:shuffle([targetSound,distractor]),attempted:false,completed:false,hadWrongAttempt:false,rewarded:false,closed:false};});soundFindSession=[...soundFindOriginal];};
 const findSession=()=>findMode==='words'?wordFindSession:soundFindSession,findOriginal=()=>findMode==='words'?wordFindOriginal:soundFindOriginal,findGoal=()=>findOriginal().length,currentFindTask=()=>findSession()[findTaskIndex],currentProgress=()=>findOriginal().filter(task=>task.closed).length;
 const updateFindProgress=()=>{const progress=currentProgress(),goal=findGoal();[...findDinoSteps.children].forEach((step,i)=>{const done=i<progress;step.classList.toggle('done',done);step.innerHTML=done?'<span class="step-check">✓</span>':'';});const pct=goal?Math.min(100,(progress/goal)*100):0;findDino.style.left=`calc(${pct}% - ${pct/100*46}px)`;};
 const buildFindProgress=()=>{findDinoSteps.replaceChildren();for(let i=0;i<findGoal();i++){const step=document.createElement('span');step.className='dino-step';findDinoSteps.append(step);}updateFindProgress();};
 const refreshFindNav=()=>{const total=findSession().length,attempted=!!currentFindTask()?.attempted;findPrev.disabled=findTaskIndex===0;findNext.disabled=!attempted;findNext.dataset.cycleEnd=String(findTaskIndex>=total-1);};
 const allFindClosed=()=>findGoal()>0&&currentProgress()===findGoal();
 const showFindCompletion=()=>{if(!allFindClosed())return;if(!findCompletion){findCompletion=document.createElement('section');findCompletion.className='finish dino-celebration games-completion';findCompletion.innerHTML=G.completionHtml;findGame.insertAdjacentElement('afterend',findCompletion);findCompletion.querySelector('.dino-again')?.addEventListener('click',startFindSession);}findGame.hidden=true;findCompletion.hidden=false;};
 const startFindRemediation=()=>{const pending=findOriginal().filter(task=>!task.closed).map(prep);if(!pending.length){setTimeout(showFindCompletion,900);return;}if(findMode==='words')wordFindSession=shuffle(pending);else soundFindSession=shuffle(pending);findTaskIndex=0;renderFindTask();};
 const finishFindAttempt=()=>refreshFindNav();
 const goNextFindTask=()=>{if(findNext.disabled)return;if(findTaskIndex<findSession().length-1){findTaskIndex++;renderFindTask();}else if(allFindClosed())showFindCompletion();else startFindRemediation();};
 const offerContinueInCard=btn=>{clearTimeout(continueTimer);continueTimer=setTimeout(()=>{if(!btn.isConnected||!btn.classList.contains('correct'))return;btn.classList.remove('learning-text','hand');btn.classList.add('find-continue');btn.innerHTML='<span>Продолжить</span><span aria-hidden="true">›</span>';btn.setAttribute('aria-label','Продолжить');btn.onclick=e=>{e.preventDefault();if(btn.dataset.navigating==='1')return;btn.dataset.navigating='1';goNextFindTask();};},420);};
 const renderFindTask=()=>{clearTimeout(continueTimer);const task=currentFindTask();if(!task)return;const target=findMode==='words'?task.targetWord:task.targetSound;findPicture.textContent=task.picture;findPicture.setAttribute('aria-label',`Послушать ${findMode==='words'?'слово':'звук'} ${target}`);answers.replaceChildren(...task.answers.map(label=>{const correct=label===target,btn=document.createElement('button');btn.type='button';btn.className='find-answer'+(task.completed&&correct?' correct':'');G.setLearningText(btn,label);btn.addEventListener('click',()=>{if(correct){answers.querySelectorAll('.find-answer').forEach(x=>x.classList.remove('try-again'));btn.classList.add('correct');if(!task.completed){const firstTry=!task.hadWrongAttempt;task.attempted=true;task.completed=true;if(!task.rewarded&&firstTry)task.rewarded=true;if(!task.closed){task.closed=true;updateFindProgress();G.celebrate(btn);}finishFindAttempt();offerContinueInCard(btn);}}else if(!task.completed){task.attempted=true;task.hadWrongAttempt=true;btn.classList.add('try-again');finishFindAttempt();}});if(task.completed&&correct)offerContinueInCard(btn);return btn;}));refreshFindNav();};
 const startFindSession=()=>{findMode==='words'?createWordFindSession():createSoundFindSession();findTaskIndex=0;hideFindCompletion();buildFindProgress();renderFindTask();};
 const setFindMode=mode=>{findMode=mode;findTaskIndex=0;hideFindCompletion();soundsMode.classList.toggle('active',mode==='sounds');wordsMode.classList.toggle('active',mode==='words');if(mode==='words'&&!wordFindOriginal.length)createWordFindSession();if(mode==='sounds'&&!soundFindOriginal.length)createSoundFindSession();buildFindProgress();renderFindTask();};
 const currentWordSet=()=>[...new Set((sessionWords||[]).slice(0,wordSessionSize))],currentSoundSet=()=>[...new Set((letterSession||[]).slice(0,letterSessionSize))];
 const sameSet=(session,items,key)=>{const targets=[...new Set(session.map(task=>task[key]))];return targets.length===items.length&&items.every(item=>targets.includes(item));};
 const restoreFindAfterParent=()=>{if(!isFindOpen)return;['readingView','lettersView','finishView','letterFinishView'].forEach(id=>{const el=document.getElementById(id);if(el)el.hidden=true;});const gamesView=document.getElementById('gamesView');if(gamesView)gamesView.hidden=false;if(gamesGrid)gamesGrid.hidden=true;if(childTabs)childTabs.hidden=true;gamesTab?.classList.add('on');const original=findOriginal(),sessionChanged=findMode==='words'?!sameSet(original,currentWordSet(),'targetWord'):!sameSet(original,currentSoundSet(),'targetSound');if(sessionChanged)startFindSession();else{hideFindCompletion();renderFindTask();updateFindProgress();}};
 const module={hide:()=>{clearTimeout(continueTimer);if(findCompletion)findCompletion.hidden=true;if(findGame)findGame.hidden=true;},close:()=>{isFindOpen=false;module.hide();},setClosed:()=>{isFindOpen=false;}};G.register(module);
 findCard?.addEventListener('click',()=>{isFindOpen=true;G.hideOtherGames(module);gamesGrid.hidden=true;findGame.hidden=false;if(childTabs)childTabs.hidden=true;findTaskIndex=0;createWordFindSession();createSoundFindSession();setFindMode(findMode);});
 allGames?.addEventListener('click',G.showGamesList);soundsMode?.addEventListener('click',()=>setFindMode('sounds'));wordsMode?.addEventListener('click',()=>setFindMode('words'));
 document.getElementById('parentBack')?.addEventListener('click',restoreFindAfterParent);
 findPrev?.addEventListener('click',()=>{if(findTaskIndex>0){findTaskIndex--;renderFindTask();}});findNext?.addEventListener('click',goNextFindTask);
 findPicture?.addEventListener('click',()=>{const task=currentFindTask();if(!task)return;const spoken=findMode==='words'?task.targetWord:task.spoken;if(typeof speak==='function')speak(spoken);});
 const style=document.createElement('style');style.textContent='#childView .find-nav-arrow.game-nav-success{background:#fff;color:#123f73;border-color:#d2deea;box-shadow:0 2px 8px #123f730d}#childView .find-nav-arrow.game-nav-success:hover{border-color:#aebfd1;background:#fbfdff}#childView .find-answer.find-continue{font-family:inherit;font-weight:800;display:flex;align-items:center;justify-content:center;gap:12px;cursor:pointer}#childView .find-answer.find-continue span:last-child{font-size:1.25em;line-height:1}';document.head.appendChild(style);
});
